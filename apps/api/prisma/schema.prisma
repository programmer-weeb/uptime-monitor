// Prisma schema — models will be added in Day 2+.
// This file exists so `prisma generate` / `prisma migrate dev` have a starting point.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
